const colors = {
  black: "#3c3c3c"
};

const spacing = {
  xs: "4px",
  s: "8px",
  m: "16px",
  l: "24px",
  xl: "48px"
};

export const theme = {
  colors,
  spacing
};

export type Theme = typeof theme;
